apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: Knicks
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/chirag-baghra/AiOPS-Project-Knicks-store.git'
    targetRevision: main
    path: gitops
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: Knicks
  syncPolicy: {}
  # syncPolicy:
  #   automated:
  #     prune: true
  #     selfHeal: true
